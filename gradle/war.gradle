apply plugin: "war"

bootWar {
    mainClass = "com.alphadevs.wikunum.services.InventoryManagementApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
